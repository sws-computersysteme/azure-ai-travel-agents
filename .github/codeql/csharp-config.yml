name: C# CodeQL Configuration

paths:
  - src/tools/customer-query/**

paths-ignore:
  - '**/obj/**'
  - '**/bin/**'
  - '**/*.Tests/**'
  - '**/TestResults/**'
  - '**/test/**'
  - '**/tests/**'

queries:
  - name: Security and quality queries for C#
    uses: security-and-quality
  - name: Security extended queries for C#
    uses: security-extended